{
  "api": {
    "type": "graphql",
    "endpoints": ["transactions", "blocks", "metrics"],
    "scaling": {
      "minReplicas": 2,
      "maxReplicas": 6
    },
    "cache": {
      "enabled": true,
      "type": "redis"
    }
  }
}